# TO-DOS

## Organize Admin and User Menus into Subgroups - 2026-02-14 13:17

- **Reorganize Sidebar menu items into logical sub-menus** - Group the flat list of admin and user nav items into collapsible/cohesive categories to reduce confusion. **Problem:** The admin section has 18 flat menu items (User Management, Feature Management, Schedule Match, Record Results, Events, Seasons, Players, Divisions, Match Config, Championships, Tournaments, Challenges, Promos, Contender Config, Fantasy Shows, Fantasy Config, Help, Danger Zone) making it overwhelming and hard to navigate. The public/user section also has 9+ items with mixed visibility based on roles and feature flags. No new features — purely UX reorganization. **Files:** `frontend/src/components/Sidebar.tsx:84-241` (all nav links — public section lines 86-167, admin section lines 170-241), `frontend/src/components/admin/AdminPanel.tsx:28-30` (tab type and valid tabs list), `frontend/src/components/Sidebar.css` (will need sub-menu styling). **Solution:** Evaluate whether admin items can be grouped into sub-menus (e.g., "Match Operations" = Schedule/Results/Match Config; "League Setup" = Players/Divisions/Seasons/Championships; "Fantasy" = Fantasy Shows/Fantasy Config; "System" = Users/Features/Danger Zone). Also evaluate whether any closely related pages (e.g., Fantasy Shows + Fantasy Config, or Challenges + Promos disabled items) should be combined into a single tabbed page rather than separate menu entries. Consider similar grouping for the public nav (e.g., "Competition" = Standings/Championships/Tournaments, "Community" = Challenges/Promos/Contenders). No backend changes needed.

## Modular Consistent Seed Data Scripts - 2026-02-14 13:19

- **Refactor seed data into modular per-feature scripts** - Break the monolithic 658-line `seed-data.ts` into separate, composable seed scripts per feature domain with cross-table data consistency. **Problem:** Current `seed-data.ts` is one massive function that seeds everything at once. Data across tables is inconsistent — e.g., match results may not align with season standings or player win/loss records. Hard to seed just one feature for testing. 16 tables are referenced (Players, Matches, Championships, ChampionshipHistory, Tournaments, Seasons, SeasonStandings, Divisions, Events, ContenderRankings, RankingHistory, FantasyConfig, WrestlerCosts, FantasyPicks, SiteConfig). **Files:** `backend/scripts/seed-data.ts:1-658` (monolithic seed script), `backend/scripts/clear-data.ts` (corresponding clear script), `backend/package.json` (npm run scripts for seed/clear). **Solution:** Create separate scripts grouped by domain (e.g., `seed-core.ts` = Players + Divisions + Seasons; `seed-matches.ts` = Matches + SeasonStandings + player W/L/D updates; `seed-championships.ts` = Championships + ChampionshipHistory; `seed-tournaments.ts`; `seed-events.ts`; `seed-fantasy.ts` = FantasyConfig + WrestlerCosts + FantasyPicks; `seed-contenders.ts`). Each script should import shared player/season IDs from a common data file to ensure consistency. A master `seed-all.ts` should orchestrate them in dependency order. Critical constraint: if a match shows Player A winning 3 matches in Season 1, the SeasonStandings and Players table must reflect those same 3 wins.

## Create OpenAPI Spec with Swagger Docs - 2026-02-14 13:22

- **Generate OpenAPI 3.0 specification and serve Swagger UI** - Create a comprehensive OpenAPI doc covering all API endpoints and serve interactive Swagger documentation. **Problem:** No API documentation exists beyond the CLAUDE.md overview. The API has 40+ endpoints across 12+ domains (auth, players, matches, championships, tournaments, standings, seasons, divisions, images, events, contenders, fantasy, site-config, admin) defined in `serverless.yml` (1466 lines) with handler implementations spread across `backend/functions/`. No existing OpenAPI or Swagger files in the project. **Files:** `backend/serverless.yml:155-900` (all HTTP event definitions with paths, methods, and authorizer config), `frontend/src/services/api.ts:1-650` (all endpoint URLs and request/response shapes), `frontend/src/types/index.ts` (TypeScript interfaces for all entities — maps to OpenAPI schemas), `backend/functions/` (handler implementations with request validation and response formats). **Solution:** Create an `openapi.yaml` (or `openapi.json`) at project root or `backend/docs/`. Define all paths, request bodies, response schemas, auth (JWT Bearer via Cognito), and error responses. Consider auto-generating from serverless.yml + TypeScript types where possible. Serve Swagger UI either as a static page (e.g., via S3/CloudFront at `/api-docs`) or as a Lambda-served endpoint. Distinguish public vs authenticated endpoints in the spec.

## Fix Local Dev Broken by Split Stacks - 2026-02-14 13:23

- **Fix local development broken by serverless-plugin-split-stacks** - Local `serverless-offline` no longer works correctly after adding the split-stacks plugin; diagnose and fix, then update README with any new local dev steps. **Problem:** `serverless-plugin-split-stacks` (v1.14.0) was added to split CloudFormation resources across nested stacks to avoid the 500-resource limit. This works for AWS deploys but breaks local development via `serverless-offline` since offline mode doesn't understand nested/split stacks. The plugin is loaded before `serverless-offline` in the plugins list (line 107 vs 108), which may cause conflicts. **Files:** `backend/serverless.yml:105-118` (plugins list: split-stacks at line 107, offline at line 108; splitStacks config at lines 111-114; serverless-offline config at lines 115-118), `backend/package.json` (npm run offline script), `README.md` (local development instructions). **Solution:** Options include: (1) conditionally disable split-stacks when running offline (e.g., use a stage-based condition or custom variable to skip the plugin locally), (2) reorder plugins so serverless-offline loads before split-stacks, (3) use `serverless-offline`'s `--noStart` or similar flags to bypass split-stack processing, or (4) use a separate serverless config for local dev. Update README local development section with any new steps or workarounds needed.
